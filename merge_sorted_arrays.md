## 题目描述

给定两个按非递减顺序排列的整数数组 nums1 和 nums2，以及它们的有效元素个数 m 和 n。
其中 nums1 的长度为 m + n，前 m 个位置存放了有效元素，后 n 个位置为 0（占位符，代表空闲空间）。

请你实现一个函数 merge，将 nums2 的所有元素合并到 nums1 中，使得合并后的数组仍然按非递减顺序排列。

**要求：**
- 不能使用额外的数组，空间复杂度为 O(1)
- 保证 `nums1` 有足够的空间来存放 nums2 的元素

### 输入
- nums1：一个长度为 m + n 的数组，其中前 m 个元素是有效元素，后 n 个元素为 0。
- nums2：一个长度为 n 的数组，包含 n 个有效元素。

### 输出
- 合并后的 nums1 数组，包含所有元素且按非递减顺序排列。
